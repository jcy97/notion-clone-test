NODE_ENV=development
PORT=3001

# MongoDB 연결 문자열 (MongoDB Atlas 또는 로컬)
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/notion-clone?retryWrites=true&w=majority

# JWT 시크릿 키 (실제 운영에서는 복잡한 랜덤 문자열 사용)
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production

# 프론트엔드 URL
FRONTEND_URL=http://localhost:3000

# 파일 업로드 설정
MAX_FILE_SIZE=10mb
UPLOAD_PATH=./uploads